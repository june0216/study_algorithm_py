SELECT P.PRODUCT_ID,P.PRODUCT_NAME,	(O.AM * P.PRICE) AS TOTAL_SALES
FROM FOOD_PRODUCT P
JOIN (
SELECT *, SUM(AMOUNT) AS AM FROM FOOD_ORDER WHERE PRODUCE_DATE	LIKE "2022-05%" GROUP BY PRODUCT_ID) O
ON O.PRODUCT_ID = P.PRODUCT_ID
GROUP BY P.PRODUCT_ID
ORDER BY TOTAL_SALES DESC , PRODUCT_ID ASC;